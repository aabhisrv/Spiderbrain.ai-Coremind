{
  "name": "spiderbrain-monorepo",
  "private": true,
  "type": "module",
  "description": "Test runner for the spiderbrain.ai packages (never published).",
  "scripts": {
    "test": "node --test \"test/conformance.test.mjs\""
  }
}
